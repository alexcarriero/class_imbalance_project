# set up
sc    = scenario
start = start
stop  = stop
out   =  c()
# save correction/algorithm pair
pair_id    <- as.character(pair[,1])
correction <- as.character(pair[,2])
algorithm  <- as.character(pair[,3])
# load data for scenario
all_the_data <- readRDS(paste0("generated_data/scenario_", sc,".rds"))
data <- all_the_data$data
test <- all_the_data$test
# pre-process step
if(correction == "rus"){ data <- lapply(data, do_rus) }
if(correction == "ros"){ data <- lapply(data, do_ros) }
# store the pre-processed data sets
saveRDS(data, paste0("sim_results/preprocessed/sc", sc, "_p", pair_id, "_", start, "_", stop,".rds"))
# implement algorithm
if(algorithm == "logistic_regression"){
for(i in start:stop){
pred  <- do_lrg(data[[i]], test[[i]], i)
out   <- rbind(out, pred)
}
}
if(algorithm == "support_vector_machine"){
for(i in start:stop){
pred <- do_svm(data[[i]], test[[i]], i)
out  <- rbind(out, pred)
}
}
out <- as.data.frame(out) %>% `rownames<-`( NULL )
# save predicted probabilities
saveRDS(out, file = paste0("sim_results/predictions/sc", sc, "_p", pair_id, "_", start, "_", stop,".rds"))
# save raw performance metrics
raw_performance_measures <-
out %>%
mutate(iter  = as.factor(iter),
class = as.factor(class)) %>%
group_by(iter) %>%
group_modify(~performance_statistics(pred = .x$pred, class = .x$class, converge = .x$convergence))
saveRDS(raw_performance_measures, paste0("sim_results/raw_performance_metrics/sc", sc, "_p", pair_id, "_", start, "_", stop,".rds"))
}
sim_in_parallel(3, pairs[8,], start = 1, stop = 2000)
pred_sc3_p8  <- readRDS("sim_results/predictions/sc3_p8_1_2000.rds")
r_pm_sc3_p8  <- readRDS("sim_results/raw_performance_metrics/sc3_p8_1_2000.rds")
r_pm_sc3_p2  <- readRDS("sim_results/raw_performance_metrics/sc3_p2_1_2000.rds")
r_pm_sc1_p8  <- readRDS("sim_results/raw_performance_metrics/sc1_p8_1_2000.rds")
r_pm_sc1_p2  <- readRDS("sim_results/raw_performance_metrics/sc1_p2_1_2000.rds")
s_pm_sc1_p2 <- get_summary(r_pm_sc1_p2, 1, pairs[2,])
s_pm_sc1_p8 <- get_summary(r_pm_sc1_p8, 1, pairs[8,])
s_pm_sc3_p2 <- get_summary(r_pm_sc3_p2, 3, pairs[2,])
s_pm_sc3_p8 <- get_summary(r_pm_sc3_p8, 3, pairs[8,])
rbind(s_pm_sc1_p2, s_pm_sc1_p8, s_pm_sc3_p2, s_pm_sc3_p8)
pred_sc3_p8  <- readRDS("sim_results/predictions/sc3_p8_1_2000.rds")
pred_sc3_p8%>%
filter(convergence != 1) %>%
ggplot(aes(x = pred, y = class, group = as.factor(iter))) +
geom_line(stat = "smooth",
method = stats::loess,
formula = y ~ x,
se = F,
linewidth = 0.05,
alpha = 0.1,
color = "#0d0887") +
geom_abline(slope = 1, intercept = 0, size = 1) +
theme_minimal() +
xlab(" ") +
ylab(" ") +
xlim(0,1) +
ylim(0,1)
# libraries
# general
library(tidyverse)
library(devtools)
library(MASS)
# pre-processing corrections
library(ROSE)
library(smotefamily)
# models
library(caret)
library(randomForest)
library(xgboost)
library(ebmc)
# simulation features
library(pROC)
library(SimDesign)
library(simsalapar)
# IRIC package
source("IRIC-master/R/Ensemble-based level/EasyEnsemble.R")
source("IRIC-master/R/Data level/SmoteENN.R")
library(RANN)
# plots
library(patchwork)
library(gridExtra)
library(ggridges)
pred_sc3_p8%>%
filter(convergence != 1) %>%
ggplot(aes(x = pred, y = class, group = as.factor(iter))) +
geom_line(stat = "smooth",
method = stats::loess,
formula = y ~ x,
se = F,
linewidth = 0.05,
alpha = 0.1,
color = "#0d0887") +
geom_abline(slope = 1, intercept = 0, size = 1) +
theme_minimal() +
xlab(" ") +
ylab(" ") +
xlim(0,1) +
ylim(0,1)
calibration_plot <- function(dataframe){
dataframe %>%
filter(convergence != 1) %>%
ggplot(aes(x = pred, y = class, group = as.factor(iter))) +
geom_line(stat = "smooth",
method = stats::loess,
formula = y ~ x,
se = F,
linewidth = 0.05,
alpha = 0.1,
color = "#0d0887") +
geom_abline(slope = 1, intercept = 0, size = 1) +
theme_minimal() +
xlab(" ") +
ylab(" ") +
xlim(0,1) +
ylim(0,1)
}
# predictions
pred_sc1_p1  <- readRDS("sim_results/predictions/sc1_p1_1_2000.rds")
calibration_plot <- function(sc, pair_id){
dataframe <- readRDS(paste0("sim_results/predictions/sc", sc, "_p", pair_id, "_", start, "_", stop,".rds"))
dataframe %>%
filter(convergence != 1) %>%
ggplot(aes(x = pred, y = class, group = as.factor(iter))) +
geom_line(stat = "smooth",
method = stats::loess,
formula = y ~ x,
se = F,
linewidth = 0.05,
alpha = 0.1,
color = "#0d0887") +
geom_abline(slope = 1, intercept = 0, size = 1) +
theme_minimal() +
xlab(" ") +
ylab(" ") +
xlim(0,1) +
ylim(0,1)
}
calibration_plot <- function(sc, pair_id, start, stop){
dataframe <- readRDS(paste0("sim_results/predictions/sc", sc, "_p", pair_id, "_", start, "_", stop,".rds"))
dataframe %>%
filter(convergence != 1) %>%
ggplot(aes(x = pred, y = class, group = as.factor(iter))) +
geom_line(stat = "smooth",
method = stats::loess,
formula = y ~ x,
se = F,
linewidth = 0.05,
alpha = 0.1,
color = "#0d0887") +
geom_abline(slope = 1, intercept = 0, size = 1) +
theme_minimal() +
xlab(" ") +
ylab(" ") +
xlim(0,1) +
ylim(0,1)
}
calibration_plot <- function(sc, pair_id, start = 1, stop = 2000){
dataframe <- readRDS(paste0("sim_results/predictions/sc", sc, "_p", pair_id, "_", start, "_", stop,".rds"))
dataframe %>%
filter(convergence != 1) %>%
ggplot(aes(x = pred, y = class, group = as.factor(iter))) +
geom_line(stat = "smooth",
method = stats::loess,
formula = y ~ x,
se = F,
linewidth = 0.05,
alpha = 0.1,
color = "#0d0887") +
geom_abline(slope = 1, intercept = 0, size = 1) +
theme_minimal() +
xlab(" ") +
ylab(" ") +
xlim(0,1) +
ylim(0,1)
}
calibration_plot(sc = 1, pair_id = 1)
deviance <- function(data, lev = NULL, model = NULL) {
obs  <- as.numeric(data$obs) - 1
pred <- data$one
pred[pred == 0] <- 1e-16
pred[pred == 1] <- 1-1e-16
dev <- -2*sum(obs*log(pred) + (1-obs)*log(1-pred))
c(Deviance = dev)
}
all_data <- readRDS("generated_data/scenario_3.rds")
data <- all_data$data
test <- all_data$test
# caret
library(caret)
i = 1
df   <- data[[i]]
test <- test[[i]]
deviance <- function(data, lev = NULL, model = NULL) {
obs  <- as.numeric(data$obs) - 1
pred <- data$one
pred[pred == 0] <- 1e-16
pred[pred == 1] <- 1-1e-16
dev <- -2*sum(obs*log(pred) + (1-obs)*log(1-pred))
c(Deviance = dev)
}
do_svm <- function(df, test, iter){
# ensure correct factor levels, and character labels
df$outcome     = factor(df$outcome, levels=c(0,1), ordered = TRUE)
levels(df$outcome) <- c("zero", "one")
# model
train_ctrl <- trainControl(method = "cv", number = 5, summaryFunction = deviance, classProbs = T)
mod        <- train(outcome ~.,
data = df,
method = "svmLinear",
metric = "deviance",
maximize = FALSE,
trControl = train_ctrl,
)
pred <- predict(mod, newdata = test, type = "prob")["one"]
colnames(pred) = NULL
# store results
pred <- cbind("pred" = pred, "iter" = iter, "class" = as.numeric(test$outcome) - 1, "convergence" = 0)
return(pred)
}
i = 4
out <- do_svm(data[[i]], test[[i]], i)
all_data <- readRDS("generated_data/scenario_3.rds")
data <- all_data$data
test <- all_data$test
do_svm <- function(df, test, iter){
# ensure correct factor levels, and character labels
df$outcome     = factor(df$outcome, levels=c(0,1), ordered = TRUE)
levels(df$outcome) <- c("zero", "one")
# model
train_ctrl <- trainControl(method = "cv", number = 5, summaryFunction = deviance, classProbs = T)
mod        <- train(outcome ~.,
data = df,
method = "svmLinear",
metric = "Deviance",
maximize = FALSE,
trControl = train_ctrl,
)
pred <- predict(mod, newdata = test, type = "prob")["one"]
colnames(pred) = NULL
# store results
pred <- cbind("pred" = pred, "iter" = iter, "class" = as.numeric(test$outcome) - 1, "convergence" = 0)
return(pred)
}
i = 4
out <- do_svm(data[[i]], test[[i]], i)
out <- as.data.frame(out) %>% `rownames<-`( NULL )
out
tunegrid <- expand.grid(
mtry = 1:8,
min.node.size = 1:10
)
do_rf <- function(df, test, iter){
# ensure correct factor levels, and character labels
df$outcome     = factor(df$outcome, levels=c(0,1), ordered = TRUE)
levels(df$outcome) <- c("zero", "one")
# model
train_ctrl <- trainControl(method = "cv", number = 5, summaryFunction = deviance, classProbs = T)
mod        <- train(outcome ~.,
data = df,
method = "ranger",
metric = "Deviance",
maximize = FALSE,
trControl = train_ctrl,
tuneGrid = tunegrid
)
pred <- predict(mod, newdata = test, type = "prob")["one"]
colnames(pred) = NULL
# store results
pred <- cbind("pred" = pred, "iter" = iter, "class" = as.numeric(test$outcome) - 1, "convergence" = 0)
return(pred)
}
do_rnf <- function(df, test, iter){
# ensure correct factor levels, and character labels
df$outcome     = factor(df$outcome, levels=c(0,1), ordered = TRUE)
levels(df$outcome) <- c("zero", "one")
# model
train_ctrl <- trainControl(method = "cv", number = 5, summaryFunction = deviance, classProbs = T)
mod        <- train(outcome ~.,
data = df,
method = "ranger",
metric = "Deviance",
maximize = FALSE,
trControl = train_ctrl,
tuneGrid = tunegrid
)
pred <- predict(mod, newdata = test, type = "prob")["one"]
colnames(pred) = NULL
# store results
pred <- cbind("pred" = pred, "iter" = iter, "class" = as.numeric(test$outcome) - 1, "convergence" = 0)
return(pred)
}
i = 4
out2 <- do_rnf(data[[i]], test[[i]], i)
tunegrid <- expand.grid(
mtry = 1:8,
min.node.size = 1:10
)
do_rnf <- function(df, test, iter){
# ensure correct factor levels, and character labels
df$outcome     = factor(df$outcome, levels=c(0,1), ordered = TRUE)
levels(df$outcome) <- c("zero", "one")
# model
train_ctrl <- trainControl(method = "cv", number = 5, summaryFunction = deviance, classProbs = T)
mod        <- train(outcome ~.,
data = df,
method = "ranger",
metric = "Deviance",
maximize = FALSE,
trControl = train_ctrl,
tuneGrid = tunegrid
)
pred <- predict(mod, newdata = test, type = "prob")["one"]
colnames(pred) = NULL
# store results
pred <- cbind("pred" = pred, "iter" = iter, "class" = as.numeric(test$outcome) - 1, "convergence" = 0)
return(pred)
}
out2 <- do_rnf(data[[i]], test[[i]], i)
tunegrid <- expand.grid(
mtry = 1:8,
min.node.size = 1:10
)
tunegrid
tunegrid <- expand.grid(
mtry = 1:8,
min.node.size = 1:10,
splitrule = "gini"
)
out2 <- do_rnf(data[[i]], test[[i]], i)
out2 <- as.data.frame(out) %>% `rownames<-`( NULL )
out2
out2 <- as.data.frame(out2) %>% `rownames<-`( NULL )
out2
out2 <- do_rnf(data[[i]], test[[i]], i)
out2 <- as.data.frame(out2) %>% `rownames<-`( NULL )
out2
do_xgb <- function(df, test, iter){
# ensure correct factor levels, and character labels
df$outcome     = factor(df$outcome, levels=c(0,1), ordered = TRUE)
levels(df$outcome) <- c("zero", "one")
# model
train_ctrl <- trainControl(method = "cv", number = 5, summaryFunction = deviance, classProbs = T)
mod        <- train(outcome ~.,
data = df,
method = "xgbTree",
metric = "Deviance",
maximize = FALSE,
trControl = train_ctrl
)
pred <- predict(mod, newdata = test, type = "prob")["one"]
colnames(pred) = NULL
# store results
pred <- cbind("pred" = pred, "iter" = iter, "class" = as.numeric(test$outcome) - 1, "convergence" = 0)
return(pred)
}
do_xgb <- function(df, test, iter){
# ensure correct factor levels, and character labels
df$outcome     = factor(df$outcome, levels=c(0,1), ordered = TRUE)
levels(df$outcome) <- c("zero", "one")
# model
train_ctrl <- trainControl(method = "cv", number = 5, summaryFunction = deviance, classProbs = T)
mod        <- train(outcome ~.,
data = df,
method = "xgbTree",
metric = "Deviance",
maximize = FALSE,
trControl = train_ctrl
)
pred <- predict(mod, newdata = test, type = "prob")["one"]
colnames(pred) = NULL
# store results
pred <- cbind("pred" = pred, "iter" = iter, "class" = as.numeric(test$outcome) - 1, "convergence" = 0)
return(pred)
}
out <- do_svm(data[[i]], test[[i]], i)
out <- as.data.frame(out) %>% `rownames<-`( NULL )
out
out <- do_rnf(data[[i]], test[[i]], i)
out <- as.data.frame(out) %>% `rownames<-`( NULL )
out
out <- do_xgb(data[[i]], test[[i]], i)
out <- as.data.frame(out) %>% `rownames<-`( NULL )
out
do_xgb <- function(df, test, iter){
# ensure correct factor levels, and character labels
df$outcome     = factor(df$outcome, levels=c(0,1), ordered = TRUE)
levels(df$outcome) <- c("zero", "one")
# model
train_ctrl <- trainControl(method = "cv", number = 5, summaryFunction = deviance, classProbs = T)
mod        <- train(outcome ~.,
data = df,
method = "xgbTree",
metric = "Deviance",
maximize = FALSE,
trControl = train_ctrl,
verbosity = 0,
verbose = FALSE
)
pred <- predict(mod, newdata = test, type = "prob")["one"]
colnames(pred) = NULL
# store results
pred <- cbind("pred" = pred, "iter" = iter, "class" = as.numeric(test$outcome) - 1, "convergence" = 0)
return(pred)
}
out <- do_xgb(data[[i]], test[[i]], i)
out <- as.data.frame(out) %>% `rownames<-`( NULL )
out
algorithms   <- c("logistic_regression", "support_vector_machine", "random_forest", "xgboost", "rusboost", "easy_ensemble")
corrections  <- c("control", "rus", "ros", "smo", "sen")
pairs <-
expand_grid(corrections, algorithms) %>%
mutate(pair_id = c(1:30)) %>%
relocate(pair_id)
pairs
all_data <- readRDS("generated_data/scenario_3.rds")
data <- all_data$data
test <- all_data$test
data <- all_data$data
test <- all_data$test
set.seed(40)
data <- lapply(data, do_rus)
# functions
source("functions.R")
# simulation scenarios
set <- readRDS("data-generating-mechanism/set.RData")
data <- lapply(data, do_rus)
data[[1]]
set.seed(40)
data <- lapply(data, do_rus)
data <- all_data$data
test <- all_data$test
set.seed(40)
data <- lapply(data, do_rus)
data[[1]]
data <- all_data$data
test <- all_data$test
set.seed(40)
data <- lapply(data, do_rus)
i = 1
out <- do_svm(data[[i]], test[[i]], i)
out <- as.data.frame(out) %>% `rownames<-`( NULL )
out
all_data <- readRDS("generated_data/scenario_3.rds")
data <- all_data$data
test <- all_data$test
all_data <- readRDS("generated_data/scenario_3.rds")
data <- all_data$data
test <- all_data$test
set.seed(40)
data <- lapply(data, do_rus)
data[[1]]
i = 1
out <- do_svm(data[[i]], test[[i]], i)
out <- as.data.frame(out) %>% `rownames<-`( NULL )
out
data[[2]]
all_data <- readRDS("generated_data/scenario_3.rds")
data <- all_data$data
test <- all_data$test
set.seed(40)
data <- lapply(data, do_rus)
data[[2]]
92.424/2 -1
pchisq(10.354, 1)
1-0.9987081
pchisq(10.354, 1, lower.tail = F)
104.35 -102.81
x <- sample(1000, 27)
x
seeds <- c(940, 615, 963, 604, 335, 505, 607, 85, 965, 529, 937, 454, 519, 226,
591, 860, 768, 120, 911, 231, 368, 788, 896, 778, 552, 419, 103)
length(seeds)
unique(seeds)
length(unique(seeds))
library(tidyverse)
library(MASS)
source("./functions.R")
getwd()
setwd("/Users/alexcarriero/Sync/thesis github/simulation/use_to_generate_data")
source("./functions.R")
library(tidyverse)
library(MASS)
# scenario number
sc <- as.numeric(Sys.getenv("SLURM_ARRAY_TASK_ID"))
# seed
sd <- seeds[sc]
sc <- 1
# seed
sd <- seeds[sc]
# seed
sd <- seeds[sc]
# generate data
set.seed(sd)
generate_all_the_data(sc, iter = 10)
scenario_1 <- readRDS("~/Sync/thesis github/simulation/use_to_generate_data/generated_data/scenario_1.rds")
View(scenario_1)
View(scenario_1)
scenario_1[["data"]]
